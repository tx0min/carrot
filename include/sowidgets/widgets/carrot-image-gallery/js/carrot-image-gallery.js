(function($){

	var windowWidth,
		windowHeight;
		
		
	/*function initSlidesWidth(slider){
		var cols=slider.data('cycle-carousel-cols');
		var colssmall=slider.data('cycle-carousel-cols-small');
		var c=cols;
		
		if(windowWidth<767)	c=colssmall;
		
		var w=slider.width()/c;
		slider.find('article').width(w+"px");
		slider.find('article.cycle-slide-active').removeClass('cycle-slide-active');
		slider.find('.cycle-carousel-wrap').css('left','0px');
		slider.find('.cycle-carousel-wrap').find('article:first-child').addClass('cycle-slide-active');
	}
	*/
	
	/*
	$(document).ready(function($){	
		windowHeight = $(window).height();
		windowWidth = $(window).width();

		initSliders();
	});
	*/
	




		
		
		
		
		
		

	
})(jQuery);